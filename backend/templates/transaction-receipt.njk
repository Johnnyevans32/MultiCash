<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Receipt</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap');

        body {
            font-family: 'Inter', Arial, sans-serif;
            color: #4a4a4a;
            margin: 0;
            padding: 40px;
            background-color: #f5f5f5;
            font-size: 14px;
        }

        h1,
        h2,
        p {
            margin: 0;
        }

        .logo {
            text-align: left;
            margin-bottom: 20px;
        }

        .section-title {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 10px;
            color: #333;
            border-bottom: 1px solid #e0e0e0;
        }

        .transfer-details,
        .personal-details,
        .footer {
            padding: 20px 0;
        }

        .info-group {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            justify-content: start;
            margin-bottom: 10px;
            color: #4a4a4a;
            margin-top: 20px;
        }

        .info-group strong {
            color: #333;
        }

        .footer {
            font-size: 12px;
            color: #888;
            margin-top: 20px;
        }

        a {
            color: #1a73e8;
            text-decoration: none;
        }
    </style>
</head>

<body>

    <!-- Logo Section -->
    <div class="logo">
        <img src="https://res.cloudinary.com/dfbjysygb/image/upload/v1730144622/xsg9oloeuv2ftcuc3g0w.png" alt="Logo"
            width="200px" height="auto">
    </div>

    <!-- Transfer Confirmation Section -->
    <div class="transfer-details">
        <div class="section-title">Transaction receipt</div>
        <div class="info-group">
            <p><strong>Transaction created</strong><br>{{ date | date('MMMM D, YYYY h:mm:ss A z') }}</p>
            <p><strong>Transaction ref</strong><br>#{{ transferReference }}</p>
        </div>
    </div>

    <!-- Your Details Section -->
    <div class="personal-details">
        <div class="section-title">Your details</div>
        <div class="info-group">
            <p><strong>Name</strong><br>{{ user.name }}</p>
        </div>
    </div>

    <!-- Transfer Overview Section -->
    <div class="transfer-details">
        <div class="section-title">Transfer overview</div>
        <div class="info-group">
            <p><strong>Amount</strong><br>{{ transactionAmount }}</p>
            <p><strong>Fees</strong><br>{{ fee }}</p>
        </div>
        <div class="info-group">
            <p><strong>Total amount paid</strong><br>{{ amount }}</p>
        </div>

    </div>

    <!-- Footer Section -->
    <div class="footer">
        <p>MultiCash is a registered payment service provider dedicated to facilitating seamless transactions across
            multiple currencies. We operate under strict compliance with relevant financial regulations, ensuring the
            highest standards of security and service. Our mission is to empower users with easy access to their funds
            while prioritizing transparency and reliability. For inquiries, please contact us at
            support@multicash.africa.
        </p>
        <p>MultiCash is committed to maintaining the highest level of customer satisfaction. Visit our website at <a
                href="https://www.multicash.africa">www.multicash.africa</a> for more information.</p>
    </div>

</body>

</html>